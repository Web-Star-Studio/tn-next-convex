## Custom Hooks

### src/hooks/use-debounce.ts

- useDebounce
  
  Example:
```tsx
import { useDebounce } from "@/hooks/use-debounce";

const value = useDebounce(/* params */);
```

### src/hooks/use-mobile.ts

- useIsMobile
  
  Example:
```tsx
import { useIsMobile } from "@/hooks/use-mobile";

const value = useIsMobile(/* params */);
```

### src/hooks/use-modal-cleanup.ts

- useForceModalCleanup
  
  Example:
```tsx
import { useForceModalCleanup } from "@/hooks/use-modal-cleanup";

const value = useForceModalCleanup(/* params */);
```

- useModalCleanup
  
  Example:
```tsx
import { useModalCleanup } from "@/hooks/use-modal-cleanup";

const value = useModalCleanup(/* params */);
```

### src/hooks/useCouponValidation.ts

- useCouponValidation
  
  Example:
```tsx
import { useCouponValidation } from "@/hooks/useCouponValidation";

const value = useCouponValidation(/* params */);
```

- useMultipleCouponsValidation
  
  Example:
```tsx
import { useMultipleCouponsValidation } from "@/hooks/useCouponValidation";

const value = useMultipleCouponsValidation(/* params */);
```

### src/hooks/useCurrentUser.ts

- useCurrentUser
  
  Example:
```tsx
import { useCurrentUser } from "@/hooks/useCurrentUser";

const value = useCurrentUser(/* params */);
```

- useIsAuthenticated
  
  Example:
```tsx
import { useIsAuthenticated } from "@/hooks/useCurrentUser";

const value = useIsAuthenticated(/* params */);
```

- useRequireAuth
  
  Example:
```tsx
import { useRequireAuth } from "@/hooks/useCurrentUser";

const value = useRequireAuth(/* params */);
```

### src/hooks/useEmployees.ts

- useEmployees
  
  Example:
```tsx
import { useEmployees } from "@/hooks/useEmployees";

const value = useEmployees(/* params */);
```

### src/hooks/useOnboarding.ts

- useOnboarding
  
  Example:
```tsx
import { useOnboarding } from "@/hooks/useOnboarding";

const value = useOnboarding(/* params */);
```

### src/hooks/useOptimizedAssets.ts

- useAssetTypeCounts
  
  Example:
```tsx
import { useAssetTypeCounts } from "@/hooks/useOptimizedAssets";

const value = useAssetTypeCounts(/* params */);
```

- useOptimizedAssets
  
  Example:
```tsx
import { useOptimizedAssets } from "@/hooks/useOptimizedAssets";

const value = useOptimizedAssets(/* params */);
```

### src/hooks/usePackageRequestQueries.ts

- usePackageRequestQueries
  
  Example:
```tsx
import { usePackageRequestQueries } from "@/hooks/usePackageRequestQueries";

const value = usePackageRequestQueries(/* params */);
```
